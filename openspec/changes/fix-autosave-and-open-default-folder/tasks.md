## 1. 自动保存逻辑修复

- [x] 1.1 梳理 `src/App.vue` 中 `saveBlobToFile` 分支，确保自动保存条件命中时不弹保存对话框
- [x] 1.2 实现自动保存失败时的错误提示与手动保存回退逻辑
- [x] 1.3 校验自动保存开关、默认目录为空、默认目录无效三种分支行为

## 2. 默认目录可点击打开

- [x] 2.1 在 Electron bridge（`preload/main`）新增打开目录 API
- [x] 2.2 将默认目录文本改为可点击交互，点击后调用打开目录能力
- [x] 2.3 增加目录打开失败提示与不可用状态禁用样式

## 3. 回归与验收

- [x] 3.1 验证“开启自动保存 + 有效目录”时截图直接落盘且无保存弹框
- [x] 3.2 验证自动保存失败回退到手动保存弹框流程
- [x] 3.3 验证点击目录可打开文件夹且不影响截图/录屏主流程
