## 1. 主题入口融合重构

- [x] 1.1 调整 `src/App.vue` 头部结构，将主题切换入口融入 header tools 区域
- [x] 1.2 重构主题切换控件尺寸、边距与对齐规则，消除突兀感
- [x] 1.3 补充窄窗口下的自适应布局，保证主题入口可达且不遮挡信息
- [x] 1.4 统一主题入口 hover/focus/active 反馈与可访问性语义

## 2. 视觉层级与质感精修

- [x] 2.1 在 `src/style.css` 优化语义 token 层级（surface/text/border/state）映射
- [x] 2.2 重做头部、主面板、分区容器的层级节奏（对比、边框、阴影、留白）
- [x] 2.3 统一按钮、输入框、源卡片等控件状态反馈，提升一致性
- [x] 2.4 调整默认主题观感为更克制的生产力工具风格，去除不协调样式

## 3. 双主题一致性与回归

- [x] 3.1 在 light/dark 下检查关键文本、边界与交互对比度
- [x] 3.2 验证主题切换过程即时生效且无布局跳变
- [x] 3.3 验证截屏/复制/录屏核心流程不受样式重构影响
- [x] 3.4 清理遗留无效样式并完成构建验证
