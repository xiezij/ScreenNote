## 1. 主题状态与切换能力

- [x] 1.1 在 `src/App.vue` 中新增 `themePreference` 状态（`light|dark|system`）及初始化逻辑
- [x] 1.2 实现 `resolvedTheme` 计算与 `document.documentElement` 的 `data-theme` 同步
- [x] 1.3 增加 `localStorage` 读写，确保主题偏好在重启后可恢复
- [x] 1.4 在 `system` 模式下监听系统主题变化并自动更新界面

## 2. 主题切换 UI 与可用性

- [x] 2.1 在页面头部增加主题切换入口（至少含浅色、深色、跟随系统三项）
- [x] 2.2 处理主题切换交互反馈，保证切换即时生效且不影响已有操作控件
- [x] 2.3 为主题切换控件补充可访问性属性（标签与可识别状态）

## 3. 视觉体系重构（去 AI 味）

- [x] 3.1 在 `src/style.css` 建立语义化设计 token（背景/文本/边框/强调/状态）
- [x] 3.2 将 `src/App.vue` 内主容器、头部、面板、表单与按钮样式迁移到 token 驱动
- [x] 3.3 替换紫黑主导视觉为中性低饱和工具风格，减少高饱和渐变依赖
- [x] 3.4 统一 toast、滚动条、状态提示在 light/dark 下的对比度与层级表现

## 4. 回归验证与交付

- [x] 4.1 在 light/dark 主题下验证截屏保存、复制剪贴板、录屏开始/停止流程
- [x] 4.2 验证 `system` 模式在系统主题切换时可自动同步
- [x] 4.3 检查关键文本和控件对比度，确保可读性与可点击性
- [x] 4.4 清理无效旧样式并确认无功能回归后提交实现
